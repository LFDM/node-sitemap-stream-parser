{
  "name": "node-sitemap-stream-parser",
  "version": "0.1.0-rc5",
  "main": "dist/index.js",
  "bin": {
    "node-sitemap-stream-parser": "dist/cli.js"
  },
  "typings": "dist/index.d.ts",
  "dependencies": {
    "got": "^9.6.0",
    "request": "^2.88.0",
    "sax": "^1.2.4",
    "typescript": "3.5.2"
  },
  "devDependencies": {
    "@types/got": "^9.6.7",
    "@types/jest": "^24.0.15",
    "@types/request": "^2.48.1",
    "@types/sax": "^1.2.0",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.3.0"
  },
  "scripts": {
    "build": "tsc --outDir dist --m CommonJs --resolveJsonModule -d src/index.ts",
    "prepublish": "npm run test && npm run build",
    "test": "jest"
  },
  "files": [
    "dist"
  ]
}
